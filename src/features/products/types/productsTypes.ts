// src/features/products/types/productsTypes.ts

/**
 * Interface representing the different authentication actions.
 */
export interface Product {
  id: number;
  name: string;
  price: number;
  description: string;
  imageUrl: string;
  // Add other product properties as needed
}
